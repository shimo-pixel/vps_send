# MinIO S3 API（不要写 http://，端口写在 MINIO_ENDPOINT 里）
MINIO_ENDPOINT=127.0.0.1:9000
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=minioadmin
# 若前面有 HTTPS 反代到 MinIO，改为 true
MINIO_SECURE=false
MINIO_BUCKET=uploads
# Flask
FLASK_HOST=0.0.0.0
FLASK_PORT=5000
